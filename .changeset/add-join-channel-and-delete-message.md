---
"slack-tools": minor
---

Add `slack_join_channel` and `slack_delete_message` tools

- `slack_join_channel`: Join a public Slack channel via `conversations.join`
- `slack_delete_message`: Delete the bot's own messages via `chat.delete`
